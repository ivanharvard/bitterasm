from std.mem import pointer

pub struct Array<T> {
    data: pointer<T>,
    length: int,
    capacity: int
}

pub macro deref<T>(arr: Array<T>) 
    | -> pointer<T>
    | syntax { *$arr }
{
    return arr.data
}