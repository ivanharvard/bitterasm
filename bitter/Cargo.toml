[package]
name = "bitter"
version = "0.2.0-alpha"
edition = "2024"

[dependencies]
bitterasm = { version = "0.2.0-alpha", path = ".." }
clap = { version = "4.6.6", features = ["derive"] }
num-bigint = "0.4"
serde_json = "1"
