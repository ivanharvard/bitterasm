[package]
name = "bitter"
version = "0.1.0"
edition = "2024"

[dependencies]
bitterasm = { version = "0.1.0", path = ".." }
clap = { version = "4.6.6", features = ["derive"] }
num-bigint = "0.4"
serde_json = "1"
