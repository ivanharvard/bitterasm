mov rax, rbx
add rcx, rdx
or r8, r9
and r10, r11
sub r12, r13
xor r14, r15
cmp rax, rbx
test rcx, rdx
shl rax, 4
shr rbx, 4
sar rcx, 4
shl rdx, cl
shr rsi, cl
sar rdi, cl
push rax
push r15
pop rbx
pop r12
