struct Bits<const width: int>
    | invariant value >= 0 && value < (1 << width)
{
    value: int
}

type UByte = Bits<8>
    | invariant value < 100

const FIVE: UByte = 5

macro take(x: UByte) {
    @emit x
}

macro use_five() {
    take FIVE
}

use_five
