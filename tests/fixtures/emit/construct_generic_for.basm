struct Array<T, const n: int> {
    @for i in 0..n {
        __el`i`: T,
    }
}

macro make_array() {
    @emit Array<int, 3> {
        @for i in 0..3 {
            __el`i`: i * 2,
        }
    }
}
