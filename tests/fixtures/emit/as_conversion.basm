struct Bits<const width: int>
    | invariant value >= 0 && value < (1 << width)
{
    value: int
}

type UByte = Bits<8> | invariant value < 100

macro make(v: int) {
    @emit v @as UByte
}

macro take(x: UByte) {
    @emit x
}
